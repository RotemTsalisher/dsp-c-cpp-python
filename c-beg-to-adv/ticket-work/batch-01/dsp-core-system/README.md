# CardoLink DSP Core — batch 01 (C11)

Ticket exercises aligned with **all 12 lines** in `course-subjects.txt`. Notebooks live in `../entry-level/`, `../mid-level/`, `../staff-level/`.

```powershell
cmake -S . -B build
cmake --build build --target dsp_ticket_test
.\build\Debug\dsp_ticket_test.exe DSP-ENTRY-101
```

See `TICKET_CROSSWALK.md` for subject mapping.
