- drawing shapes with prints
- variables
- data types (integers, double, float, char, string)
- printf function (general usage, format specifires)
- working with numbers (basic math operations, combining integers and floating points, ontegers with floating point results, basic c math functions {not libraries}, )
- constants
- user inputs (integers, double, float, character, string, strings with white spaces {fgets})
- milestone (1) project with all of the so far established knowledge
- milestone (2) building a small game with all the current knowledge
- static arrays (integers, doubles, floats, chars and strings)
- static arrays : using user inputs
- functions : empty params, params, void return, other type return, math functions, complex functions
- if statements
- logic statements and comparison operators
- milestone (3) project with all of the so far established knowledge
- switch case
- structs
- while loops
- milestone (4) project with all of the so far established knowledge
- for loops
- 2D arrays
- Nested Loops
