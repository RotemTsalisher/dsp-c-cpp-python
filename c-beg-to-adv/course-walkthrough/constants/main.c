#include <stdio.h>

int main() {

    /* there is a practice to name constants with CAPITAL LETTERS */
    const int NUM = 5;
    printf("num = %d\n", NUM);

    /* cannot change constants in runtime */
    //num = 2;
    return 0;
}