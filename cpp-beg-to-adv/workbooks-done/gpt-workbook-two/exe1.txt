/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, Java, PHP, Ruby, Perl,
C#, OCaml, VB, Swift, Pascal, Fortran, Haskell, Objective-C, Assembly, HTML, CSS, JS, SQLite, Prolog.
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
#include <print>
#include <iostream>

namespace logicA {
    int transform(int a) {
        std::cout << "logicA::transform(int): " << std::endl;
        return a * 10;
    };
    
    double transform(double a) {
        std::cout << "logicA::transform(dobule): " << std::endl;
        return a * 10;
    };
}

namespace logicB {
    int transform(int a){
        std::cout << "logicB::transform(int): " << std::endl;
        return a * 20;
    };
    
    double transform(double a){
        std::cout << "logicB::transform(double): " << std::endl;
        return a* 20;
    };
    
}
int main()
{
    int n = 1; 
    double x = 1.0;
    
    int int_res = 0;
    double double_res = 0;
    
    int_res = logicA::transform(n);
    std::cout << "int_res = " << int_res << std::endl;
    
    double_res = logicA::transform(x);
    std::cout << "double_res = " << double_res << std::endl;
    
    int_res = logicB::transform(n);
    std::cout << "int_res = " << int_res << std::endl;
    
    double_res = logicB::transform(x);
    std::cout << "double_res = " << double_res << std::endl;
    return 0;
}