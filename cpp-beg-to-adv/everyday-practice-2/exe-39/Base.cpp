#include "Base.h"

double Base::binWeight(int k, bool normalized) const {
    std::cout << "Base::binWeight(int k, bool normalized = true)" << std::endl;
    std::cout << "args: k = " << k << " , notmalized = " << normalized << std::endl;
    return 1.0;
};